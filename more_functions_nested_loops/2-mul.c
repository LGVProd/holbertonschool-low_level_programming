#include "main.h"
#include <stdio.h>
/**
 * mul - main function
 * @a: first integer
 * @b: second integer
 * Return: result
 * Multiply two integers.
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
