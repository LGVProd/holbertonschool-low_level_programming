#include "main.h"
#include <stdio.h>
/**
 * add - main function
 * Add two integers and return the result
 * @a: first integer
 * @b: second integer
 * Return: result
 */
int add(int a, int b)
{
	int result;

	result = (a + b);
	return (result);
}
