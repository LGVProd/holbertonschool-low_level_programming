#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs - main function (absolute value of a number)
 * @c: input value
 * Return: absolute value of c
 * Print the absolute value of an integer.
 */
int _abs(int c)
{
	int x;

	x = abs(c);
	return (x);
}
